server {
    listen 80;
    server_name icivi.online;

    root /var/www/icivi.online;

    location ^~ /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
